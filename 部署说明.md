# 《冷海筑新：北境转型》静态部署说明

## 包内容

- 入口文件：`index.html`
- 游戏逻辑：`game.js`
- 页面样式：`styles.css`
- 离线缓存：`sw.js`
- Web App 配置：`manifest.webmanifest`
- 资源目录：`assets/`
  - 项目与实体卡图片：全部为本地 WebP 文件
  - 图标与 favicon：`app-icon.svg`、`app-icon-192.png`、`app-icon-512.png`
  - 本地二维码生成器：`qr-generator.js`
  - 单文件离线版：`leng-hai-offline.html`

项目没有独立字体文件，也不请求网络字体。页面使用设备已有的中文及系统字体回退栈，因此无需另行上传字体资源。

## 运行方式

### 直接打开

解压后双击根目录的 `index.html` 即可游玩。此方式不需要安装软件，也不需要联网。

浏览器通过本地文件打开时，游戏功能、图片和实体卡概念页均可用。受浏览器安全规则限制，离线缓存安装、在线网址复制和部署地址二维码不会启用；这些功能会在静态服务器环境下自动启用。

若希望只分发一个文件，可直接使用 `assets/leng-hai-offline.html`。该文件已内嵌脚本、样式和图片。

### 部署到静态服务器

1. 将压缩包完整解压。
2. 把解压后的全部文件和 `assets/` 目录原样上传，不能只上传 `index.html`。
3. 将站点默认首页（Index Document）设置为 `index.html`。
4. 建议开启 HTTPS，以启用离线缓存和安装到桌面功能。
5. 如服务商要求手动设置 MIME 类型，请确保：
   - `.js` 为 `text/javascript` 或 `application/javascript`
   - `.webp` 为 `image/webp`
   - `.svg` 为 `image/svg+xml`
   - `.webmanifest` 为 `application/manifest+json`

所有页面和资源均使用相对路径，可部署在域名根目录或任意子目录。分享面板会根据当前访问地址现场生成二维码，不包含固定域名。

## 服务器端接口

不需要服务器端接口、数据库、账号系统、密钥或第三方 API。任意能够托管静态文件的中国大陆云存储、对象存储或静态网站服务均可运行。

`sw.js` 只访问本站同源文件，用于浏览器离线缓存；不连接外部接口。若部署平台不支持 Service Worker，游戏仍可正常运行，只是不提供自动离线缓存。

## 发布前检查

- 访问首页并完成一次 3 人或 4 人开局。
- 打开“分享与离线”，确认二维码指向当前部署地址。
- 检查手机端项目卡、规则、教学与实体卡概念页。
- 在浏览器开发者工具中确认没有资源 404 和网站自身的 JavaScript 报错。
- 如在中国大陆公开提供互联网信息服务，请按所选云服务商和部署地区的要求完成域名备案、公安备案等合规流程。
